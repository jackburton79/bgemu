# Custom starting character spec (see -c/--character).
# Half-Elf Fighter/Cleric - same "warrior priest" role ANOMEN10 filled,
# but a character of your own instead of the premade NPC.

name Kestrel
gender MALE
race HALF_ELF
class FIGHTER_CLERIC
alignment LG

str 17
dex 14
con 15
int 10
wis 15
chr 12

# A small starting priest spellbook (level 1).
spell SPPR103
spell SPPR104

# No portrait set - add a line like "portrait <resref>" (without the
# S/M suffix, both get added automatically) if you want a specific one.
